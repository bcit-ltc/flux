# Cloudnative-pg + Vault Secrets Operator dynamic secrets

Cloudnative-pg provisions a Postgres cluster and app databases using short‑lived DB users generated by Vault.

## Usage

Use the kubectl plugin to check the cluster health:

    krew install cnpg
    kubectl cnpg status pg-core -n postgres

## Adding new app databases

    kubectl apply -f fluxRoot/infrastructure/cloudnative-pg/scripts/bootstrap-app-privs-job.yaml
