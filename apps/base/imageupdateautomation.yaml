# yaml-language-server: $schema=https://raw.githubusercontent.com/fluxcd-community/flux2-schemas/refs/heads/main/imageupdateautomation-image-v1beta2.json
---
# Clones, updates, and commits new versions to app charts
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageUpdateAutomation
metadata:
  name: image-automation
spec:
  interval: 24h
  sourceRef:
    kind: GitRepository
    name: git-repository
  git:
    commit:
      author:
        email: fluxcdbot@issues.ltc.bcit.ca
        name: fluxcdbot
      messageTemplate: |-
        Automated image update
                
        Changes:
        {{ range .Changed.Changes -}}
        - {{ .OldValue }} -> {{ .NewValue }}
        {{ end -}}

        Files:
        {{ range $filename, $_ := .Changed.FileChanges -}}
        - {{ $filename }}
        {{ end -}}
        [skip ci]
    push:
      branch: main

    # For auto-PR's, see https://fluxcd.io/flux/use-cases/gh-actions-auto-pr/
    #      branch: staging
  update:
    strategy: Setters
  suspend: false
