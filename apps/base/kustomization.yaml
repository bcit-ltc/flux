# Base kustomization for all app workloads
---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - helm-release.yaml
  - namespace.yaml
  - oci-repository.yaml

# Teach kustomize how to update generated names
configurations:
  - kustomizeconfig.yaml
