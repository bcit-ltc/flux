# Deploys Ingress resources
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-web
spec:
  tls:
    - hosts:
        - app.web.ltc.bcit.ca
      secretName: star-ltc-bcit-ca
  rules:
    - host: app.web.ltc.bcit.ca
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: app
                port:
                  number: 8080
