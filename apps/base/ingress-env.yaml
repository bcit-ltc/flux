# Deploys Ingress resources
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-env
spec:
  tls:
    - hosts:
        - app.env.ltc.bcit.ca
      secretName: star-latest-ltc-bcit-ca
  rules:
    - host: app.env.ltc.bcit.ca
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: app
                port:
                  number: 8080
